# demo-svc Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a containerised Python/Flask web service that serves a live dark-mode dashboard showing its own runtime info, demonstrating the container-in-container capability of the claude-devpod devcontainer.

**Architecture:** Flask app with three routes (`/`, `/api/status`, `/health`). A before/after request hook logs each request into an in-memory circular buffer. The dashboard HTML polls `/api/status` every 2 seconds via `fetch()` and updates the DOM in place. The whole service is packaged as a Podman container built from UBI9.

**Tech Stack:** Python 3, Flask, pytest, Podman, UBI9

---

## Base Directory

All work happens inside the devcontainer. The sub-project root is:
```
container_in_container/demo/
```
All file paths below are relative to this directory. All `git` commands are run from this directory.

---

## File Map

| File | Responsibility |
|---|---|
| `app.py` | Flask app — routes, request log buffer, system info collection |
| `templates/index.html` | Dashboard page — inline CSS, inline JS polling loop |
| `tests/test_app.py` | pytest tests for all routes and request logging |
| `requirements.txt` | Flask (runtime dependency) |
| `requirements-dev.txt` | pytest (test-only dependency) |
| `Dockerfile` | Inner container image — UBI9 + Python + Flask |
| `.gitignore` | Ignore `__pycache__`, `.pytest_cache`, `*.pyc` |
| `README.md` | Build and run instructions |

---

## Task 1: Initialize project structure

**Files:**
- Create: `container_in_container/demo/` (new standalone git repo)
- Create: `requirements.txt`
- Create: `requirements-dev.txt`
- Create: `.gitignore`
- Create: `tests/__init__.py`

- [ ] **Step 1: Create the directory and initialize a git repo**

```bash
mkdir -p /workspaces/claude-devpod/container_in_container/demo/tests
cd /workspaces/claude-devpod/container_in_container/demo
git init
```

Expected: `Initialized empty Git repository in .../container_in_container/demo/.git/`

- [ ] **Step 2: Create requirements.txt**

```
flask
```

- [ ] **Step 3: Create requirements-dev.txt**

```
pytest
```

- [ ] **Step 4: Create .gitignore**

```
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
```

- [ ] **Step 5: Create tests/__init__.py**

Empty file — marks `tests/` as a Python package.

```bash
touch tests/__init__.py
```

- [ ] **Step 6: Install dependencies**

```bash
pip3 install -r requirements.txt -r requirements-dev.txt
```

Expected: Flask and pytest install without errors.

- [ ] **Step 7: Verify pytest runs (empty suite)**

```bash
python3 -m pytest tests/ -v
```

Expected: `no tests ran` or `0 passed`.

- [ ] **Step 8: Commit**

```bash
git add .
git commit -m "chore: initialize demo-svc project structure"
```

---

## Task 2: Flask skeleton + /health endpoint

**Files:**
- Create: `app.py`
- Create: `tests/test_app.py`

- [ ] **Step 1: Write the failing tests**

Create `tests/test_app.py`:

```python
import pytest
from unittest.mock import patch


@pytest.fixture
def client():
    with patch("app.PODMAN_VERSION", "5.0.0-test"), \
         patch("app.OS_NAME", "Test OS 1.0"):
        import app as app_module
        app_module.app.config["TESTING"] = True
        app_module.REQUEST_LOG.clear()
        with app_module.app.test_client() as c:
            yield c


def test_health_returns_200(client):
    res = client.get("/health")
    assert res.status_code == 200


def test_health_returns_json(client):
    res = client.get("/health")
    data = res.get_json()
    assert data == {"status": "ok"}


def test_request_is_logged_after_call(client):
    import app as app_module
    client.get("/health")
    assert len(app_module.REQUEST_LOG) == 1
    entry = app_module.REQUEST_LOG[0]
    assert entry["method"] == "GET"
    assert entry["path"] == "/health"
    assert entry["status"] == 200
    assert isinstance(entry["duration_ms"], int)
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
python3 -m pytest tests/test_app.py -v
```

Expected: `ERROR` — `ModuleNotFoundError: No module named 'app'`

- [ ] **Step 3: Create app.py with skeleton, request logging, and /health**

```python
import subprocess
import time
from collections import deque

from flask import Flask, g, jsonify, render_template, request

app = Flask(__name__)

START_TIME = time.time()
REQUEST_LOG = deque(maxlen=20)


def _get_podman_version():
    try:
        out = subprocess.check_output(["podman", "--version"], text=True).strip()
        return out.split()[-1]
    except Exception:
        return "unavailable"


def _get_os_name():
    try:
        with open("/etc/os-release") as f:
            for line in f:
                if line.startswith("PRETTY_NAME="):
                    return line.split("=", 1)[1].strip().strip('"')
    except Exception:
        pass
    return "unknown"


PODMAN_VERSION = _get_podman_version()
OS_NAME = _get_os_name()


@app.before_request
def _before():
    g.start = time.time()


@app.after_request
def _after(response):
    duration_ms = round((time.time() - g.start) * 1000)
    REQUEST_LOG.appendleft(
        {
            "method": request.method,
            "path": request.path,
            "status": response.status_code,
            "duration_ms": duration_ms,
        }
    )
    return response


@app.route("/health")
def health():
    return jsonify({"status": "ok"})


if __name__ == "__main__":
    app.run(host="0.0.0.0", port=8080, debug=False)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
python3 -m pytest tests/test_app.py -v
```

Expected:
```
test_app.py::test_health_returns_200 PASSED
test_app.py::test_health_returns_json PASSED
test_app.py::test_request_is_logged_after_call PASSED
3 passed
```

- [ ] **Step 5: Commit**

```bash
git add app.py tests/test_app.py
git commit -m "feat: add Flask skeleton, request logging, and /health endpoint"
```

---

## Task 3: /api/status endpoint

**Files:**
- Modify: `app.py`
- Modify: `tests/test_app.py`

- [ ] **Step 1: Add failing tests for /api/status**

Append to `tests/test_app.py`:

```python
def test_status_returns_200(client):
    res = client.get("/api/status")
    assert res.status_code == 200


def test_status_response_shape(client):
    res = client.get("/api/status")
    data = res.get_json()
    assert isinstance(data["hostname"], str)
    assert isinstance(data["python_version"], str)
    assert isinstance(data["flask_version"], str)
    assert data["podman_version"] == "5.0.0-test"
    assert data["os"] == "Test OS 1.0"
    assert data["port"] == 8080
    assert isinstance(data["uptime_seconds"], int)
    assert data["uptime_seconds"] >= 0
    assert isinstance(data["recent_requests"], list)


def test_status_recent_requests_shows_logged_calls(client):
    import app as app_module
    app_module.REQUEST_LOG.clear()
    client.get("/health")
    res = client.get("/api/status")
    data = res.get_json()
    paths = [r["path"] for r in data["recent_requests"]]
    assert "/health" in paths
```

- [ ] **Step 2: Run tests to verify new tests fail**

```bash
python3 -m pytest tests/test_app.py -v
```

Expected: the three new tests fail with `KeyError` or `404`.

- [ ] **Step 3: Add /api/status route to app.py**

Add these imports at the top of `app.py` (after existing imports):

```python
import platform
import socket

import flask as flask_module
```

Add the route after the `/health` route:

```python
@app.route("/api/status")
def status():
    return jsonify(
        {
            "hostname": socket.gethostname(),
            "python_version": platform.python_version(),
            "flask_version": flask_module.__version__,
            "podman_version": PODMAN_VERSION,
            "os": OS_NAME,
            "port": 8080,
            "uptime_seconds": round(time.time() - START_TIME),
            "recent_requests": list(REQUEST_LOG)[:10],
        }
    )
```

- [ ] **Step 4: Run all tests to verify they pass**

```bash
python3 -m pytest tests/test_app.py -v
```

Expected:
```
test_health_returns_200 PASSED
test_health_returns_json PASSED
test_request_is_logged_after_call PASSED
test_status_returns_200 PASSED
test_status_response_shape PASSED
test_status_recent_requests_shows_logged_calls PASSED
6 passed
```

- [ ] **Step 5: Commit**

```bash
git add app.py tests/test_app.py
git commit -m "feat: add /api/status endpoint with runtime info"
```

---

## Task 4: Dashboard HTML + / route

**Files:**
- Create: `templates/index.html`
- Modify: `app.py`
- Modify: `tests/test_app.py`

- [ ] **Step 1: Add failing test for / route**

Append to `tests/test_app.py`:

```python
def test_index_returns_200(client):
    res = client.get("/")
    assert res.status_code == 200


def test_index_returns_html(client):
    res = client.get("/")
    assert b"demo-svc" in res.data
    assert b"api/status" in res.data
```

- [ ] **Step 2: Run to verify they fail**

```bash
python3 -m pytest tests/test_app.py::test_index_returns_200 tests/test_app.py::test_index_returns_html -v
```

Expected: both `FAILED` with 404.

- [ ] **Step 3: Create templates/ directory and index.html**

```bash
mkdir -p templates
```

Create `templates/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>demo-svc</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      min-height: 100vh;
      background: linear-gradient(160deg, #1a1a2e 0%, #16213e 55%, #0f3460 100%);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      color: #e2e8f0;
      padding: 32px 24px;
    }

    .container { max-width: 720px; margin: 0 auto; }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 22px;
      padding-bottom: 18px;
      border-bottom: 1px solid rgba(255,255,255,.08);
    }
    .header-title { display: flex; align-items: center; gap: 10px; margin-bottom: 4px; }
    .header-title h1 { font-size: 20px; font-weight: 700; letter-spacing: -.02em; }
    .header-path { color: #a8b2d8; font-size: 11px; font-family: monospace; }

    .status-pill {
      display: flex; align-items: center; gap: 5px;
      padding: 5px 12px; border-radius: 20px;
      font-size: 11px; font-weight: 500; transition: all .3s;
    }
    .status-pill.running {
      background: rgba(52,211,153,.1);
      border: 1px solid rgba(52,211,153,.25);
      color: #34d399;
    }
    .status-pill.reconnecting {
      background: rgba(251,191,36,.1);
      border: 1px solid rgba(251,191,36,.25);
      color: #fbbf24;
    }
    .status-dot { width: 7px; height: 7px; border-radius: 50%; background: currentColor; box-shadow: 0 0 8px currentColor; }

    .panels { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 10px; }

    .panel { background: rgba(255,255,255,.04); border-radius: 8px; padding: 14px; }
    .panel.accent { border: 1px solid rgba(233,69,96,.25); }
    .panel.muted  { border: 1px solid rgba(168,178,216,.15); }

    .panel-label { font-size: 9px; text-transform: uppercase; letter-spacing: .12em; font-weight: 700; margin-bottom: 10px; }
    .panel.accent .panel-label { color: #e94560; }
    .panel.muted  .panel-label { color: #a8b2d8; }

    .panel-row { display: flex; justify-content: space-between; align-items: center; margin-bottom: 7px; }
    .panel-row:last-child { margin-bottom: 0; }
    .panel-key { color: #a8b2d8; font-size: 11px; }

    .badge {
      background: rgba(233,69,96,.15); border: 1px solid rgba(233,69,96,.3);
      color: #e94560; font-size: 10px; font-family: monospace; padding: 2px 7px; border-radius: 4px;
    }
    .plain-val  { color: #e2e8f0; font-size: 10px; font-family: monospace; }
    .muted-val  { color: #a8b2d8; font-size: 10px; font-family: monospace; }

    .bottom-row { display: grid; grid-template-columns: auto 1fr; gap: 10px; margin-bottom: 10px; }

    .uptime-box {
      background: rgba(255,255,255,.04); border: 1px solid rgba(168,178,216,.15);
      border-radius: 8px; padding: 12px 18px;
      display: flex; align-items: center; gap: 12px; white-space: nowrap;
    }
    .uptime-label { color: #a8b2d8; font-size: 11px; }
    .uptime-value { color: #34d399; font-size: 14px; font-weight: 700; font-family: monospace; }

    .breadcrumb {
      background: rgba(255,255,255,.04); border: 1px solid rgba(168,178,216,.15);
      border-radius: 8px; padding: 12px 14px;
      display: flex; align-items: center; gap: 6px; font-size: 10px; overflow: hidden;
    }
    .crumb       { color: #a8b2d8; white-space: nowrap; }
    .crumb-sep   { color: rgba(168,178,216,.3); }
    .crumb-acc   { color: #e94560; }
    .crumb-active{ color: #34d399; font-weight: 600; white-space: nowrap; }

    .log-panel {
      background: rgba(0,0,0,.25); border: 1px solid rgba(168,178,216,.1);
      border-radius: 8px; padding: 14px;
    }
    .log-label { color: #a8b2d8; font-size: 9px; text-transform: uppercase; letter-spacing: .12em; font-weight: 700; margin-bottom: 10px; }
    .log-entry { display: flex; gap: 10px; align-items: center; font-family: monospace; font-size: 10px; margin-bottom: 5px; }
    .log-entry:last-child { margin-bottom: 0; }
    .log-method   { color: #34d399; width: 32px; flex-shrink: 0; }
    .log-path     { color: #e2e8f0; flex: 1; }
    .log-status   { color: #34d399; }
    .log-duration { color: #4a5568; }
    .log-empty    { color: #4a5568; font-size: 11px; font-style: italic; }
  </style>
</head>
<body>
  <div class="container">

    <div class="header">
      <div>
        <div class="header-title">
          <span>🐳</span>
          <h1>demo-svc</h1>
        </div>
        <div class="header-path">container_in_container/demo · Python web service</div>
      </div>
      <div id="status-pill" class="status-pill running">
        <div class="status-dot"></div>
        <span id="status-text">Running</span>
      </div>
    </div>

    <div class="panels">
      <div class="panel accent">
        <div class="panel-label">Runtime</div>
        <div class="panel-row"><span class="panel-key">Python</span><span class="badge"    id="python-version">—</span></div>
        <div class="panel-row"><span class="panel-key">Flask</span> <span class="badge"    id="flask-version">—</span></div>
        <div class="panel-row"><span class="panel-key">Listening</span><span class="muted-val" id="listening">—</span></div>
      </div>
      <div class="panel muted">
        <div class="panel-label">Host</div>
        <div class="panel-row"><span class="panel-key">Hostname</span><span class="plain-val" id="hostname">—</span></div>
        <div class="panel-row"><span class="panel-key">OS</span>      <span class="plain-val" id="os">—</span></div>
        <div class="panel-row"><span class="panel-key">Podman</span>  <span class="plain-val" id="podman-version">—</span></div>
      </div>
    </div>

    <div class="bottom-row">
      <div class="uptime-box">
        <span class="uptime-label">Uptime</span>
        <span class="uptime-value" id="uptime">—</span>
      </div>
      <div class="breadcrumb">
        <span class="crumb-acc">⬡</span><span class="crumb">devcontainer</span>
        <span class="crumb-sep">›</span>
        <span class="crumb-acc">⬡</span><span class="crumb">podman run</span>
        <span class="crumb-sep">›</span>
        <span class="crumb-acc" style="color:#34d399">⬡</span><span class="crumb-active">demo-svc</span>
      </div>
    </div>

    <div class="log-panel">
      <div class="log-label">Recent Requests</div>
      <div id="request-log"><span class="log-empty">No requests yet</span></div>
    </div>

  </div>
  <script>
    function formatUptime(s) {
      const h = Math.floor(s / 3600), m = Math.floor((s % 3600) / 60), sec = s % 60;
      if (h > 0) return h + 'h ' + m + 'm ' + sec + 's';
      if (m > 0) return m + 'm ' + sec + 's';
      return sec + 's';
    }

    function setStatus(ok) {
      const pill = document.getElementById('status-pill');
      pill.className = 'status-pill ' + (ok ? 'running' : 'reconnecting');
      document.getElementById('status-text').textContent = ok ? 'Running' : 'Reconnecting\u2026';
    }

    function setText(id, val) { document.getElementById(id).textContent = val || '\u2014'; }

    function renderLog(reqs) {
      const el = document.getElementById('request-log');
      if (!reqs || reqs.length === 0) {
        el.innerHTML = '<span class="log-empty">No requests yet</span>';
        return;
      }
      el.innerHTML = reqs.map(r =>
        '<div class="log-entry">' +
          '<span class="log-method">'   + r.method       + '</span>' +
          '<span class="log-path">'     + r.path         + '</span>' +
          '<span class="log-status">'   + r.status       + '</span>' +
          '<span class="log-duration">' + r.duration_ms  + 'ms</span>' +
        '</div>'
      ).join('');
    }

    async function fetchStatus() {
      try {
        const res = await fetch('/api/status');
        if (!res.ok) throw new Error();
        const d = await res.json();
        setStatus(true);
        setText('python-version', d.python_version);
        setText('flask-version',  d.flask_version);
        setText('listening',      '0.0.0.0:' + d.port);
        setText('hostname',       d.hostname);
        setText('os',             d.os);
        setText('podman-version', d.podman_version);
        setText('uptime',         formatUptime(d.uptime_seconds));
        renderLog(d.recent_requests);
      } catch (_) {
        setStatus(false);
      }
    }

    fetchStatus();
    setInterval(fetchStatus, 2000);
  </script>
</body>
</html>
```

- [ ] **Step 4: Add the / route to app.py**

Add after the `/health` route and before the `/api/status` route:

```python
@app.route("/")
def index():
    return render_template("index.html")
```

- [ ] **Step 5: Run all tests**

```bash
python3 -m pytest tests/test_app.py -v
```

Expected:
```
test_health_returns_200 PASSED
test_health_returns_json PASSED
test_request_is_logged_after_call PASSED
test_status_returns_200 PASSED
test_status_response_shape PASSED
test_status_recent_requests_shows_logged_calls PASSED
test_index_returns_200 PASSED
test_index_returns_html PASSED
8 passed
```

- [ ] **Step 6: Commit**

```bash
git add app.py templates/index.html tests/test_app.py
git commit -m "feat: add dashboard HTML and / route"
```

---

## Task 5: Dockerfile

**Files:**
- Create: `Dockerfile`

- [ ] **Step 1: Create Dockerfile**

```dockerfile
FROM registry.access.redhat.com/ubi9/ubi:latest

RUN dnf install -y python3 python3-pip && dnf clean all

WORKDIR /app

COPY requirements.txt .
RUN pip3 install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8080

CMD ["python3", "app.py"]
```

- [ ] **Step 2: Build the image**

```bash
podman build -t demo-svc .
```

Expected: image builds successfully, last line reads something like `Successfully tagged localhost/demo-svc:latest`.

- [ ] **Step 3: Run the container**

```bash
podman run --rm -d -p 8080:8080 --name demo-svc-test demo-svc
```

Expected: container ID printed, no errors.

- [ ] **Step 4: Verify endpoints from inside the devcontainer**

```bash
curl -s http://localhost:8080/health
```

Expected: `{"status":"ok"}`

```bash
curl -s http://localhost:8080/api/status | python3 -m json.tool
```

Expected: JSON with all fields — `hostname`, `python_version`, `flask_version`, `podman_version`, `os`, `port`, `uptime_seconds`, `recent_requests`.

- [ ] **Step 5: Open browser on Windows host**

Navigate to `http://localhost:8080`. The dark-mode dashboard should load and live-update every 2 seconds.

- [ ] **Step 6: Stop the test container**

```bash
podman stop demo-svc-test
```

- [ ] **Step 7: Commit**

```bash
git add Dockerfile
git commit -m "feat: add Dockerfile for inner container build"
```

---

## Task 6: README

**Files:**
- Create: `README.md`

- [ ] **Step 1: Create README.md**

```markdown
# demo-svc

A minimal Python/Flask web service that runs as a Podman container inside the
claude-devpod devcontainer, demonstrating container-in-container.

Open `http://localhost:8080` in your browser to see a live dark-mode dashboard
showing the service's runtime environment.

## Prerequisites

- claude-devpod devcontainer running (provides Podman)

## Build and run

From inside the devcontainer terminal:

\`\`\`bash
cd container_in_container/demo
podman build -t demo-svc .
podman run --rm -p 8080:8080 demo-svc
\`\`\`

Then open http://localhost:8080 in your browser on the Windows host.

## Endpoints

| Endpoint | Description |
|---|---|
| `GET /` | Live dashboard (browser) |
| `GET /api/status` | JSON runtime snapshot |
| `GET /health` | Liveness check |

## Run tests

\`\`\`bash
pip3 install -r requirements-dev.txt
python3 -m pytest tests/ -v
\`\`\`

## Stop

`Ctrl+C` in the terminal running `podman run`, or:

\`\`\`bash
podman stop $(podman ps -q --filter name=demo-svc)
\`\`\`
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add README with build, run, and test instructions"
```
